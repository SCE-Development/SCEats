services:
  backend:
    build: ./backend
    volumes:
      - snacks-data:/app/data
  frontend:
    build: ./client/checkout
  sceats-nginx:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "3001:80"

volumes:
  snacks-data:


